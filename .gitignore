# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Runtime state: the database and its WAL, anchor logs, and backup copies. Ignore
# the directory rather than individual files, so a new artefact here (a backup, an
# anchor log, a key file) is never published by default.
data/

# Local agent/editor scratch space
.zcode/
.claude/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
